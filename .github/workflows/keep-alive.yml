name: Keep Render alive

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -sf --max-time 30 https://akhand-vr9h.onrender.com/api/places?limit=1 || echo "Backend waking up"
